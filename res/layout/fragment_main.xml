<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.assignment3.MainActivity$PlaceholderFragment" >

    <!-- input text box -->
    <EditText 
        android:id="@+id/text_box"
        android:layout_width="fill_parent"
        android:layout_height="50dp" 
        android:background="@android:drawable/editbox_background_normal"
        android:hint="@string/text"/>
    
    <!-- decimal button layout -->
    <Button
        android:id="@+id/decimal"
        android:layout_width="90dp"
        android:layout_height="35dp"
        android:layout_below="@id/text_box"
        android:background="@drawable/red_rounded_button"
        android:layout_marginRight="5dp"
        android:layout_marginTop="50dp"
        android:layout_toLeftOf="@+id/binary"
        android:textColor="#0000FF"
        android:radius="10dp"
        android:text="@string/decimal" />
    
    <!-- Binary Button Layout -->
    <Button
        android:id="@+id/binary"
        android:layout_width="90dp"
        android:layout_height="35dp"
        android:layout_below="@id/text_box"
        android:layout_toLeftOf="@+id/hex"
        android:layout_marginLeft="5dp"
        android:layout_marginRight="5dp"
        android:layout_marginTop="50dp"
        android:textColor="#0000FF"
        android:background="@drawable/red_rounded_button"
        android:text="@string/binary" />
        
    <!-- Hex Button Layout -->
    <Button
        android:id="@+id/hex"
        android:layout_width="90dp"
        android:layout_height="35dp"
        android:layout_below="@id/text_box"
        android:layout_alignParentRight="true"
        android:layout_marginLeft="5dp"
        android:layout_marginTop="50dp"
        android:textColor="#0000FF"
        android:background="@drawable/red_rounded_button"
        android:text="@string/hex" />
            

</RelativeLayout>
